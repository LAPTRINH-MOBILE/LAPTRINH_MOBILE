<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.APP_WordPulse">
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.APP_WordPulse">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.APP_WordPulse">
        </activity>

        <activity android:name=".features.story.StoryLevelActivity" />
        <activity android:name=".features.story.StoryTopicActivity" />
        <activity android:name=".features.story.StoryQuizActivity" />
        <activity android:name=".features.story.StoryResultActivity" />
        <activity android:name=".features.vocabulary.VocabTopicActivity" />
        <activity android:name=".features.vocabulary.FlashcardActivity" />
        <activity android:name=".features.grammar.GrammarResultActivity" />
        <activity android:name=".features.grammar.GrammarLevelSelectActivity" />
        <activity android:name=".features.grammar.GrammarActivity" />
        <activity android:name=".features.exercise.LessonListActivity" />
        <activity android:name=".features.exercise.DictationActivity" />
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".auth.RegisterActivity" />
    </application>

</manifest>
